<Window x:Class="BrainstormBuddy.CoachMarkOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="#01000000"
        ShowInTaskbar="False" ResizeMode="NoResize"
        Title="BrainstormBuddy — обучение" FontFamily="Segoe UI"
        TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType">
    <!-- Background #01000000 = практически невидимо, но ловит клики (никакого затемнения экрана). -->
    <Window.Resources>
        <!-- Золотистый акцент тура — чтобы не сливался с индиго-интерфейсом. -->
        <SolidColorBrush x:Key="CoachGold" Color="#F4C542"/>
        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource BgMediumBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrightBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextBlock.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BorderBrush}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BgBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource CoachGold}"/>
            <Setter Property="Foreground" Value="#12182A"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="{StaticResource CoachGold}"/>
        </Style>
    </Window.Resources>

    <Canvas x:Name="Root" Background="#01000000">
        <!-- Рамка-подсветка вокруг реального контрола (свечение, скругление). Без затемнения. -->
        <Border x:Name="Highlight" Visibility="Collapsed" IsHitTestVisible="False"
                BorderBrush="{StaticResource CoachGold}" BorderThickness="3" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect Color="#F4C542" BlurRadius="22" ShadowDepth="0" Opacity="0.95"/>
            </Border.Effect>
        </Border>

        <!-- Выноска с пояснением -->
        <Border x:Name="Callout" Visibility="Collapsed" MaxWidth="380"
                Background="{DynamicResource PanelBrush}" BorderBrush="{StaticResource CoachGold}"
                BorderThickness="1" CornerRadius="12" Padding="16">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="26" ShadowDepth="3" Opacity="0.55"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock x:Name="TitleText" FontSize="15" FontWeight="SemiBold" TextWrapping="Wrap"
                           Foreground="{StaticResource CoachGold}"/>
                <TextBlock x:Name="BodyText" Margin="0,7,0,14" FontSize="13" TextWrapping="Wrap"
                           Foreground="{DynamicResource TextBrush}"/>
                <DockPanel>
                    <TextBlock x:Name="StepText" DockPanel.Dock="Left" VerticalAlignment="Center"
                               FontSize="11.5" Foreground="{DynamicResource TextDimBrush}"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="SkipBtn" Content="Пропустить" Click="OnSkip"/>
                        <Button x:Name="BackBtn" Content="Назад" Click="OnBack" Margin="8,0,0,0"/>
                        <Button x:Name="NextBtn" Content="Далее" Style="{StaticResource PrimaryButton}"
                                Click="OnNext" Margin="8,0,0,0"/>
                    </StackPanel>
                </DockPanel>
            </StackPanel>
        </Border>
    </Canvas>
</Window>
